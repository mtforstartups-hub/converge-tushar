{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "bun run typecheck && bun --filter \"*\" --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "bun run typecheck:libs && bun --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "workspaces": [
    "artifacts/*",
    "lib/*",
    "lib/integrations/*",
    "scripts"
  ],
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
